[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-loom"
version = "1.0.0rc1"
description = "Generate native Claude Code and Codex agent definitions from one canonical catalog."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{name = "Brian Grubba"}]
keywords = ["ai", "agents", "claude", "codex", "developer-tools"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Software Development :: Code Generators",
]

[project.urls]
Homepage = "https://github.com/ceponatia/agent-loom"
Repository = "https://github.com/ceponatia/agent-loom"
Issues = "https://github.com/ceponatia/agent-loom/issues"

[project.scripts]
agent-loom = "agent_loom.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
agent_loom = [
  "presets/*/*",
  "presets/*/*/*",
  "presets/*/*/*/*",
  "presets/*/*/*/*/*",
]
